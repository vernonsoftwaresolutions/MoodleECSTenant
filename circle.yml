--- 
machine: 
  python: 
    version: "2.7.11"
  ruby: 
    version: "2.4.0"
  services: 
    - docker

dependencies: 
  override: 
    - "docker info"
 #   - "docker build --rm=false -t vernonsoftwaresolutions/moodlesingleinstance:$CIRCLE_BUILD_NUM ."

test: 
  post: 
  #  - "docker run -d -p 80:80 -e \"VIRTUAL_HOST=localhost\" --name moodle vernonsoftwaresolutions/moodlesingleinstance:$CIRCLE_BUILD_NUM; sleep 10"
 ##   - "docker ps"
  #  - "docker images"
  #  - "docker port moodle"
    - "curl -L www.google.com"
deployment: 
  development: 
    branch: master
    commands: 
      - "docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS"
    #  - "docker push vernonsoftwaresolutions/moodlesingleinstance:$CIRCLE_BUILD_NUM"
      - "aws configure set default.region us-east-1"
      - "aws configure set default.output json"
      - "aws cloudformation package --template-file moodletenant.yml --output-template-file moodletenant-output.yml --s3-bucket circleci.deployables"
      - "aws cloudformation deploy --template-file moodletenant-output.yml --capabilities CAPABILITY_IAM --stack-name moodle-ecs-tenant --parameter-overrides VpcId=vpc-c7aa77be Version=$CIRCLE_BUILD_NUM ecscluster=moodle-ecs-single-ECSCluster-NT8A0OQVUWDK ecslbarn=arn:aws:elasticloadbalancing:us-east-1:417615409974:loadbalancer/app/ECSLB/ad672c9dd6109d02 ecslbdnsname=ECSLB-175177459.us-east-1.elb.amazonaws.com ecslbhostedzoneid=Z35SXDOTRQ7X7K DNSName=moodle.vssdevelopment.com HostedZoneName=vssdevelopment.com.  ClientName=bradlarsen"


      #DockerRespository=vernonsoftwaresolutions/moodlesingleinstance
